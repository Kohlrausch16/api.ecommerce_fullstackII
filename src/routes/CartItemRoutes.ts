/*import { Router } from "express";
import CartItemController from "../Controller/CartItemController";

const cartItemRouter = Router();
const cartItemController = new CartItemController();

cartItemRouter.get('/carrinho', cartItemController.getCartItems);
cartItemRouter.get('/carrinho/:id', cartItemController.getCartItemById);
cartItemRouter.post('/carrinho', cartItemController.addCartItem);
cartItemRouter.put('/carrinho/:id', cartItemController.updateCartItem);
cartItemRouter.delete('/carrinho/:id', cartItemController.deleteCartItem);
cartItemRouter.patch('/carrinho/:id/status', cartItemController.patchCartItemStatus);

export default cartItemRouter;*/